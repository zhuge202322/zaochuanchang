import type { Metadata } from "next";
import type { ReactNode } from "react";
import { SiteFooter } from "@/components/SiteFooter";
import { SiteHeader } from "@/components/SiteHeader";
import "./globals.css";

export const metadata: Metadata = {
  title: {
    default: "JIABO Marine Instruments",
    template: "%s | JIABO Marine Instruments",
  },
  description:
    "Marine pressure instruments, level measurement products, valve control systems and monitoring alarm cabinets from Taizhou Jiabo Instrument Technology Co., Ltd.",
};

export default function RootLayout({ children }: Readonly<{ children: ReactNode }>) {
  return (
    <html lang="en">
      <body>
        <SiteHeader />
        {children}
        <SiteFooter />
      </body>
    </html>
  );
}
